import { View, Text, Button } from "react-native";

interface IProps {
  setModalVisible: (visible: boolean) => void;
}

export default function TopSectionAllLists({ setModalVisible }: IProps) {
  return (
    <View>
      <Text>aowefnwef</Text>
    </View>
  );
}
